export enum MeetingRouter {
  Lobby = "/meeting",
  Settings = "settings/:roomId",
  Room = "room/:roomId",
}

export enum AppRouters {
  Root = "/",
  Login = "/login",
  Meeting = "/meeting",
  Apartment = "/apartment",
}
